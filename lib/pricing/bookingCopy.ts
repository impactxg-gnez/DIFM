/** User-facing strings safe to import from client bundles (no fs / excel / Node-only deps). */

export const REVIEW_QUOTE_MESSAGE =
    "This job looks more complex than a standard booking. We'll review it and share a custom quote shortly.";

export const REVIEW_QUOTE_ETA = '30-60 minutes';

export const MANUAL_REVIEW_FALLBACK_CTA =
    "Can't find the right service or doesn't this quite match your job? Tell us what you need and we'll review it and get back to you.";
